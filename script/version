#!/usr/bin/env bash

function version {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  local ver_pipenv27="$(pipenv27 --version 2>/dev/null | awk '{print $3}' || true)"
  jq -n --arg pipenv27 "$ver_pipenv27" '{pipenv27: $pipenv27}'
}

version "$@"
